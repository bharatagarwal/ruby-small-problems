# What does the each method in the following program return after it is finished executing?

x = [1, 2, 3, 4, 5]

x.each do |a|
end

# returns the object being iterated upon
# an iterator without a block returns the an object of the Enumerator class